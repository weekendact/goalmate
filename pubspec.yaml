name: goalmate
description: "AI를 이용해 자기 개발을 돕는 애플리케이션"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'


dependencies:
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  cloud_firestore: ^4.13.0
  provider: ^6.1.1
  flutter_bloc: ^8.1.3
  cupertino_icons: ^1.0.6
  flutter:
    sdk: flutter


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
